#include <stdio.h>


int main() {
    int a;
    scanf("%d",&a);
    for (i=1,i<=a,i++){
        printf('*')
        for (j=1,j<=a,i++){
            printf('*')
        }
    }
    return 0;
}