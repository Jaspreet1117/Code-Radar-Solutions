#include <stdio.h>
#include <string.h>

int main() {
    int str[90];
    scanf("%s",str);
   int len = strlen(str);
    /*int count=0;
    for(int i =0;i<len;i++){
        if(str[i]=='a' || str[i]=='e' || str[i]=='u' || str[i]=='o' || str[i]=='I' || str[i]=='i'|| str[i]=='U' || str[i]=='A' || str[i]=='E' || str[i]=='O'){
              count++;
        }
    }*/
    printf("%d",count);*/
    return 0;
}